{% extends 'base.html.twig' %}

{% block title %}Blog Carl{% endblock %}

{% block body %}
    {% for post in posts %}
        <div>
            <a href="article/{{ post.id}}">{{ post.content }}</a>
        </div>
    {% endfor %}
{% endblock %}
